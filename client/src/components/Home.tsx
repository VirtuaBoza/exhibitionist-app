import * as React from "react";
import ClientCreate from "./ClientCreate";

const Home: React.FC<{}> = (props) => {
  return (
    <div>
      <ClientCreate />
    </div>
  );
};

export default Home;
